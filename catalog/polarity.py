"""
catalog/polarity.py
===================
Полярностни решения за lens health scoring — ЕДИНЕН източник на истина (BG).

Механизмът е портнат от `dashboards/eu-macro-dashboard/catalog/polarity.py`;
методологията е в `dashboards/macro-satellite/LENS_SCORING_METHODOLOGY.md` §3.

Типове:
  +1                        линейна, нагоре = по-здраво (растеж, доверие)
  −1                        линейна, нагоре = по-нездраво (безработица, лихви)
  ("U", "self")             U около собствената 10-г. норма
  ("U", "target", X)        U около фиксирана цел — инфлацията около X%
  ("OPT", lo, hi, s)        ОПТИМАЛНА ЗОНА (мандат №47): плато на здравето в
                            [lo, hi], спад извън нея по 1σ на всеки `s`
                            процентни пункта. **АБСОЛЮТНА** — медианата и
                            MAD-скалата на нормата НЕ участват.

Защо U за инфлацията: наивното „ниско = добре" прави дефлацията отличник.
Честният уред мери ОТКЛОНЕНИЕ от целта в двете посоки. От 01.01.2026 България
е в еврозоната → целта е ЕЦБ-целта, същият център като EA.

Защо OPT за бум-сериите: наивното „повече = по-здраво" прави БУМА отличник.
10-годишният робастен z мери „бързо ли е спрямо близкото минало", не „здраво
ли е" — в бум прозорец нормата сама се вдига и уредът аплодира прегряването.
Котвите са АБСОЛЮТНИ, затова важат и назад във времето (реконструираната
история няма look-ahead).

╔════════════════════════════════════════════════════════════════════════════╗
║ БУМ-ПОЛЯРНОСТИТЕ — РЕШЕНИ с мандат №47 (28.07.2026)                        ║
╚════════════════════════════════════════════════════════════════════════════╝

ФИКСИРАНИТЕ зони (данни-пас на живо, 28.07.2026 — праговете НЕ се преоткриват
и НЕ се калибрират наново; всяко пипане иска нов мандат):

| Ключ               | lo  | hi | s  | Provenance на hi                        |
|--------------------|-----|----|----|-----------------------------------------|
| BG_LOANS_NFC       |   0 | 12 |  6 | номиналният БВП ръст (правилото „кредит |
|                    |     |    |    | трайно над номиналния ръст = бум");     |
|                    |     |    |    | 2015-19 max 6.1 мълчи, 2005-08 min 19.4 |
|                    |     |    |    | свети                                   |
| BG_LOANS_HH        |   0 | 12 |  6 | същото; 2015-19 max 11.2 мълчи; сега    |
|                    |     |    |    | 21.0 СВЕТИ                              |
| BG_HPI             |   0 | 10 |  4 | дългосрочният доходен ръст; 2015-19 max |
|                    |     |    |    | 9.0 мълчи; сега 14.8 СВЕТИ              |
| BG_WAGES           |   0 | 13 |  5 | конвергентният таван (2015-19 max 12.1  |
|                    |     |    |    | мълчи); 2005-08 med 15.3 свети          |
| BG_PERMITS (4Q)    | −20 | 40 | 15 | 2015-19 max 38.7 мълчи; 2005-08 med     |
|                    |     |    |    | 39.8 / p75 51.4 свети                   |

Разпределенията зад тях (епохални медиани/максимуми на ТРАНСФОРМИРАНИТЕ
стойности, от кеша + БНБ seed-а):

| Серия                    | 2005-08          | 2015-19 (спокойната) | сега |
|--------------------------|------------------|----------------------|------|
| NFC г/г                  | med 54.7 max 72.4| med 2.0  max 6.1     | 11.9 |
| HH г/г                   | med 43.7 max 52.3| med 6.0  max 11.2    | 21.0 |
| HPI г/г                  | med 24.7 max 34.6| med 6.4  max 9.0     | 14.8 |
| WAGES г/г                | med 15.3 max 21.8| med 8.6  max 12.1    | 11.5 |
| PERMITS г/г 4Q-плъзгаща  | med 39.8 max 114 | med 20.9 max 38.7    | 34.6 |

СУРОВОТО г/г на PERMITS е негодно за котва (2015-19 p90=47.7, max=61.1 — шумът
гаси всяка зона), затова серията мина на трансформация `yoy_roll4` (мандат №47
§А2). Номиналният БВП ръст, който дава hi=12 на двата заема, е взет ЕДНОКРАТНО
за данни-паса (`namq_10_gdp?geo=BG&unit=CP_MEUR&na_item=B1GQ&s_adj=SCA`, живо
28.07.2026) и НЕ влиза в каталога: мед 7.7% в спокойната епоха, ~12% сега
(инфлационно надут).

ПРИЕМНИЯТ ГЕЙТ (репликация върху пълната история, 28.07.2026 — жив като
`tests/test_temperature.py::test_the_acceptance_gate_...`):
  · 2006H2-2008: max 5/5, средно 3.6, 9 от 11 тримесечия с ≥3 горещи → СВЕТИ
  · 2015-2019:   НУЛА горещи тримесечия (0/20, max 0) → МЪЛЧИ (перфектно)
  · 2009-2014:   средно 0.1 — шокът е СТУДЕН, не горещ (верният прочит)
  · 2020-2023:   средно 1.5, върхове 3/5 — вълната се навива
  · сега:        2/5 (HH + HPI); 2025Q2 удари 3/5 (+WAGES)

Двоен смисъл на изхода: ТЕМПЕРАТУРАТА брои САМО над hi (прегряването). Под lo е
криза/кредитен крънч — тя се чете в score-а (който пада и в двете посоки), не в
термометъра. Виж `analysis/temperature.py`.
"""
from __future__ import annotations

from typing import Any

INFLATION_TARGET = 2.0  # % — ЕЦБ цел (БГ е в еврозоната от 01.01.2026)
U_BAND = 1.0            # толерантна лента в σ, преди здравето да падне

POLARITY: dict[str, Any] = {
    # ── GROWTH ────────────────────────────────────────────────────────────────
    "BG_GDP_YOY": +1,
    "BG_INDPRO": +1,
    "BG_CONS": +1,
    "BG_ESI": +1,

    # ── INFLATION (U около целта ~2%) ─────────────────────────────────────────
    "BG_HICP": ("U", "target", INFLATION_TARGET),
    "BG_HICP_CORE": ("U", "target", INFLATION_TARGET),

    # ── LABOR ─────────────────────────────────────────────────────────────────
    "BG_UNRATE": -1,          # висока безработица = нездраво
    # Заплатите: РЕШЕНО с №47. Растежът на компенсациите е здраве до
    # конвергентния таван и прегряване над него (заплатно-ценова спирала) —
    # затова оптимална зона, не линейно +1.
    "BG_WAGES": ("OPT", 0.0, 13.0, 5.0),

    # ── EXTERNAL ──────────────────────────────────────────────────────────────
    "BG_CA_GDP": +1,          # по-силен баланс = по-здраво
    "BG_TRADE_GS": +1,        # по-силен търговски баланс = по-здраво

    # ── CREDIT ────────────────────────────────────────────────────────────────
    "BG_LT_RATE": -1,         # по-висока цена на дълга = по-стегнато/стрес
    # Цената на НОВИЯ фирмен кредит (мандат №42): по-скъп кредит = по-стегнати
    # условия за фирмите. Същата посока като доходността, но друг канал —
    # затова живее в СВОЯ peer-група (`lending_cost`), не при `yields`.
    "BG_LENDING_RATE": -1,
    # Заемният ръст: РЕШЕНО с №47. Котвата на правилото е номиналният БВП ръст —
    # кредит трайно над него надува баланси, не производство.
    "BG_LOANS_NFC": ("OPT", 0.0, 12.0, 6.0),
    "BG_LOANS_HH": ("OPT", 0.0, 12.0, 6.0),

    # ── PROPERTY (мандат №43; бум-полярностите РЕШЕНИ с №47) ──────────────────
    # Цените и разрешителните минават на оптимални зони: растеж в крак с дохода
    # е здраве, а над зоната е прегряване (свръхпредлагане, ипотечна уязвимост).
    # Строителната продукция ОСТАВА +1 — тя е текуща реална активност, не цена
    # на актив и не тръба; нейният бум е производство, не балансов надут.
    "BG_HPI": ("OPT", 0.0, 10.0, 4.0),
    "BG_CONSTR": +1,
    "BG_PERMITS": ("OPT", -20.0, 40.0, 15.0),

    # ── FISCAL (мандат №50) ───────────────────────────────────────────────────
    # Салдото: +1. Знаковата конвенция на B9 е „Net lending (+) / net borrowing
    # (−)“, т.е. плюсът Е излишъкът. По-близо до излишък = по-здраво; линейно,
    # без зона — фискалните котви (Маастрихт и приятели) са МЕНЮ-кандидат след
    # присъдата на П4, а НЕ прагове в скоринга.
    "BG_GOV_BALANCE": +1,
    #
    # Дългът: НИВО с −1 — решението „ниво срещу делта“, взето в рецептата.
    # Изкушението е да мерим ТЕМПА (тримесечната делта), защото „дългът се
    # качва“ звучи като въпрос за промяна. Но робастният z спрямо плъзгащата
    # 10-годишна медиана ВЕЧЕ мери дрейфа спрямо собствената норма: 28.5% при
    # десетилетна медиана около 23% дава положителен z, който казва точно
    # „нивото се е откъснало от десетилетието си“. Делтата отгоре би добавила
    # само тримесечния шум (ревизии, тайминг на емисиите), без да добави
    # информация. Затова: `transform: level` + полярност −1.
    #
    # ЧЕСТНОСТТА, която върви с това: абсолютните 28.5% са сред НАЙ-НИСКИТЕ в
    # ЕС. Уредът мери ДРЕЙФА спрямо собствената норма, не европейската класация
    # — Маастрихтските 3% / 60% са КОНТЕКСТ в текста, не прагове тук.
    "BG_GOV_DEBT": -1,
}

# Provenance на всяка зона — МАШИННО ЧЕТИМИЯТ близнак на таблицата в докстринга.
# Числото пътува с обяснението си до лицето и до briefing_context: ако утре
# някой мести праг, той мести и изречението, което го оправдава.
OPT_PROVENANCE: dict[str, str] = {
    "BG_LOANS_NFC": (
        "номиналният БВП ръст (правилото „кредит трайно над номиналния ръст = "
        "бум“); 2015-19 max 6.1 мълчи, 2005-08 min 19.4 свети"
    ),
    "BG_LOANS_HH": (
        "същата котва като фирмения кредит; 2015-19 max 11.2 мълчи, сега 21.0 свети"
    ),
    "BG_HPI": (
        "дългосрочният доходен ръст; 2015-19 max 9.0 мълчи, сега 14.8 свети"
    ),
    "BG_WAGES": (
        "конвергентният таван (2015-19 max 12.1 мълчи); 2005-08 med 15.3 свети"
    ),
    "BG_PERMITS": (
        "4-тримесечната плъзгаща: 2015-19 max 38.7 мълчи, 2005-08 med 39.8 / "
        "p75 51.4 свети (суровото г/г е негодно — шумът гаси зоната)"
    ),
}

# Кога и как са фиксирани — един стринг, цитиран от лицето и от експорта.
OPT_SOURCE_NOTE = (
    "Зоните са фиксирани от данни-пас на живо (28.07.2026) и не се калибрират "
    "наново: 2007-08 свети 4-5 от 5, 2015-2019 мълчи 0 от 20 тримесечия."
)

# Multi-lens override (лещово-специфична полярност). Празно за БГ — всяка серия
# живее в точно една леща. Механизмът се пази заради фамилния паритет.
POLARITY_BY_LENS: dict[tuple[str, str], Any] = {}

# Peer-group тегла. При 10 серии групите са по 1 серия → всички 1.0 (мандат №38
# §А3). Механизмът се пази, за да не се преправя агрегацията при Фаза 3.
PEER_GROUP_WEIGHT: dict[str, float] = {}


def polarity_for(key: str, lens: str | None = None) -> Any:
    """Полярността на серия (по избор — в контекста на конкретна леща)."""
    if lens is not None and (lens, key) in POLARITY_BY_LENS:
        return POLARITY_BY_LENS[(lens, key)]
    return POLARITY.get(key, +1)


def peer_group_weight(pg: str) -> float:
    """Тегло на peer-група в лещата. Default 1.0."""
    return PEER_GROUP_WEIGHT.get(pg, 1.0)


def is_u_shaped(polarity: Any) -> bool:
    """True за U-полярност (лента около цел или около собствената норма)."""
    return isinstance(polarity, tuple) and bool(polarity) and polarity[0] == "U"


def is_opt(polarity: Any) -> bool:
    """True за bounded-optimum полярност `("OPT", lo, hi, s)` (мандат №47)."""
    return (
        isinstance(polarity, tuple)
        and len(polarity) == 4
        and polarity[0] == "OPT"
    )


def opt_zone(polarity: Any) -> tuple[float, float, float]:
    """`("OPT", lo, hi, s)` → (lo, hi, s) като числа. Едно място за разопаковането."""
    _, lo, hi, s = polarity
    return float(lo), float(hi), float(s)


def opt_keys() -> list[str]:
    """Ключовете с оптимална зона — В РЕДА на POLARITY.

    ЕДИНСТВЕНИЯТ източник за „кои са бум-сериите": температурният слой се
    извежда оттук, не от собствен списък (иначе шестата OPT серия утре щеше да
    влезе в скоринга и да липсва в термометъра).
    """
    return [key for key, pol in POLARITY.items() if is_opt(pol)]
